{
  "scripts": {
    "postinstall": "cd client && npm install",
    "heroku-postbuild": "cd client && npm run build"
  },
  "engines": {
    "node": "18.*.*",
    "npm": "9.*.*"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/Cypress"
  ],
  "dependencies": {
    "vue-select": "^3.20.3"
  }
}
